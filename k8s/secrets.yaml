apiVersion: v1
kind: Secret
metadata:
  name: smart-router-secrets
  namespace: voice-system
  labels:
    app: smart-router
type: Opaque
stringData:
  REDIS_URL: "redis://10.100.0.4:6379"
  VOICE_AGENT_BASE_URL: "wss://buddy.breezelabs.app"
# Updated with actual cluster values:
# - Redis is at 10.100.0.4:6379 (internal cluster IP)
# - External domain for WebSocket URLs returned to providers
